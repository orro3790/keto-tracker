// import { createSelector } from 'reselect';

export const selectAlertModal = (state) => state.alertModal.modal;

// export const selectAlertModal = createSelector(
//   [selectAlertModal],
//   (selectAlertModal) => selectAlertModal
// );
